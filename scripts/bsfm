#!/usr/bin/env bash
# Compatibility alias for the ESP-IDF build/static/flash/monitor workflow.
set -euo pipefail
SCRIPT_DIR="$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)"
exec "$SCRIPT_DIR/bsfm-idf" "$@"
