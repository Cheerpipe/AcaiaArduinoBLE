diff -ruN a/src/utility/ATT.cpp b/src/utility/ATT.cpp
--- a/src/utility/ATT.cpp	2026-08-20 16:41:43
+++ b/src/utility/ATT.cpp	2026-08-20 16:41:43
@@ -653,10 +653,17 @@
 
     _cnf = false;
 
-    HCI.sendAclPkt(_peers[i].connectionHandle, ATT_CID, indicationLength, indication);
+    if (HCI.sendAclPkt(_peers[i].connectionHandle, ATT_CID, indicationLength, indication) < 0) {
+      continue;
+    }
 
-    while (!_cnf) {
+    // Bound indication confirm wait to ATT operation timeout (default 5 s; Shot Stopper sets 1 s).
+    for (unsigned long start = millis(); !_cnf; ) {
       HCI.poll();
+
+      if ((millis() - start) >= _timeout) {
+        break;
+      }
 
       if (!connected(_peers[i].addressType, _peers[i].address)) {
         break;
diff -ruN a/src/utility/HCI.cpp b/src/utility/HCI.cpp
--- a/src/utility/HCI.cpp	2026-08-20 16:41:43
+++ b/src/utility/HCI.cpp	2026-08-20 16:41:43
@@ -635,7 +635,11 @@
 
 int HCIClass::sendAclPkt(uint16_t handle, uint8_t cid, uint8_t plen, void* data)
 {
-  while (_pendingPkt >= _maxPkt) {
+  // Bound ACL credit wait; ATT/GAP callers treat a failed write as disconnectable.
+  for (unsigned long start = millis(); _pendingPkt >= _maxPkt; ) {
+    if ((millis() - start) >= 1000) {
+      return -1;
+    }
     poll();
   }
 
diff -ruN a/src/utility/HCIVirtualTransport.cpp b/src/utility/HCIVirtualTransport.cpp
--- a/src/utility/HCIVirtualTransport.cpp	2026-08-20 16:41:43
+++ b/src/utility/HCIVirtualTransport.cpp	2026-08-20 16:41:43
@@ -30,9 +30,14 @@
 {
 }
 
+#ifndef HCI_VHCI_IO_TIMEOUT_MS
+#define HCI_VHCI_IO_TIMEOUT_MS 1000
+#endif
+
 static int notify_host_recv(uint8_t *data, uint16_t length)
 {
-  xStreamBufferSend(rec_buffer,data,length,portMAX_DELAY);  // !!!potentially waiting forever
+  // Bound the VHCI RX callback: drop if the host is not draining within 1 s.
+  (void)xStreamBufferSend(rec_buffer, data, length, pdMS_TO_TICKS(HCI_VHCI_IO_TIMEOUT_MS));
   return 0;
 }
 
@@ -49,8 +54,19 @@
 
   while(true){
     length = xStreamBufferReceive(send_buffer,mybuf,258,portMAX_DELAY);
-    while (!esp_vhci_host_check_send_available()) {}
-    esp_vhci_host_send_packet(mybuf, length);
+    if (length == 0) {
+      continue;
+    }
+    const TickType_t start = xTaskGetTickCount();
+    while (!esp_vhci_host_check_send_available()) {
+      if ((xTaskGetTickCount() - start) >= pdMS_TO_TICKS(HCI_VHCI_IO_TIMEOUT_MS)) {
+        break;
+      }
+      vTaskDelay(1);
+    }
+    if (esp_vhci_host_check_send_available()) {
+      esp_vhci_host_send_packet(mybuf, length);
+    }
   }
 }
 
@@ -125,7 +141,8 @@
 int HCIVirtualTransportClass::read()
 {
   uint8_t c;
-  if(xStreamBufferReceive(rec_buffer, &c, 1, portMAX_DELAY)) {
+  // poll() already waited for availability; bound any residual receive.
+  if (xStreamBufferReceive(rec_buffer, &c, 1, pdMS_TO_TICKS(HCI_VHCI_IO_TIMEOUT_MS))) {
     return c;
   }
   return -1;
@@ -133,8 +150,7 @@
 
 size_t HCIVirtualTransportClass::write(const uint8_t* data, size_t length)
 {
-  size_t result = xStreamBufferSend(send_buffer,data,length,portMAX_DELAY);
-  return result;
+  return xStreamBufferSend(send_buffer, data, length, pdMS_TO_TICKS(HCI_VHCI_IO_TIMEOUT_MS));
 }
 
 HCIVirtualTransportClass HCIVirtualTransport;
