# Manual Test Plan

Perform all relay and machine circuit tests on a bench first. Do not connect machine circuit to the machine until the electrical checks pass.

| ID | Procedure | Expected result |
| --- | --- | --- |
| M01 | Power the controller with the paddle OFF. | Relay is open at boot; after debounce, state is `READY`. |
| M02 | Power it with paddle ON, then release it. | Machine circuit stays open until a stable OFF is detected. |
| M03 | From Ready, turn paddle ON for less than the rinse gesture threshold, then OFF. | Rinse starts, machine circuit remains closed for configured rinse duration, then opens. |
| M04 | Hold paddle ON past the rinse gesture with a connected scale. | State becomes `BREW` quickly (after debounce/BLE); first drops beep when coffee starts (independent of the retare window). |
| M05 | Repeat M04 with beep disabled. | Brew starts normally and no first-drops beep command is sent. |
| M06 | Turn paddle OFF during brew/manual. | Machine circuit opens immediately; next cycle requires normal rearm. |
| M07 | Leave a cycle ON until the configured wall time. | Machine circuit opens no later than the configured limit, never later than 60 s. |
| M08 | Disconnect the scale during brew, then reconnect it and resume valid notifications. | Weight control becomes `SUSPENDED` without silently changing the workflow to manual, then returns to `ACTIVE` only after three coherent samples from the new connection generation. Paddle OFF and hard limits always open the machine circuit. |
| M09 | On a deliberately opt-in build with `SHOT_STOPPER_ENABLE_REMOTE_MACHINE_CONTROL=1`, unlock **Admin**, open Home, and use **Start shot** / **Stop shot**. | Without unlock, Home shows the version footer and no Actions panel; APIs return `ADMIN_LOCKED`. After unlock, idle shows Start shot; an active shot shows Stop shot. The cycle follows normal state-machine behavior. On the default build both close-producing controls stay disabled and are rejected by the API. |
| M10 | Change a workflow field during brew, rinse, and manual states. | UI is disabled and API rejects each change; current snapshot is unchanged. |
| M11 | Save valid workflow settings while Ready, restart, and reopen UI. | Values persist and apply to the next cycle only. |
| M12 | Enter a relationship-invalid timing configuration. | Server rejects the complete transaction; no field changes. |
| M13 | Boot without STA credentials; leave SoftAP idle for more than 3 minutes. | AP remains reachable at `http://192.168.4.1/` (no idle SoftAP shutdown). |
| M14 | Save valid STA credentials (DHCP) and restart. | Full-screen wait overlay counts down from 180 s and hides Reload; SoftAP stops after STA associates; Serial prints DHCP address; overlay reclaims the UI at that address and the pending config is confirmed. |
| M14b | Save valid static IP and restart; stay on the wait overlay or open the new IP within 3 min. | STA uses configured IP; status shows static mode; config becomes CONFIRMED. Overlay reconnects if the address is unchanged. |
| M14c | Save unreachable/wrong static IP (or leave the wait overlay until 0 s). | Overlay keeps polling after the countdown hits 0. Pending config reverts to last-known-good (or clears STA); overlay reconnects on the previous address when it returns. STA retries continue when credentials remain. SoftAP auto-raise only if STA never joined this boot; otherwise recover with USB `AP_START` or reboot. |
| M15 | Save invalid/unreachable STA credentials and restart. | After the attempt timeout, SoftAP is available and STA association retries continue while SoftAP stays up. |
| M15b | With confirmed STA online, disable the home AP / force a link drop for less than ~15 s, then restore it. | SoftAP does not appear; STA recovers and Web UI stays on the STA IP. |
| M15c | With confirmed STA online, disable the home AP for more than ~15 s. | SoftAP does **not** appear; STA association retries continue. SoftAP recovery requires USB `AP_START` or a reboot (boot SoftAP path). |
| M16 | Scan Wi-Fi in Ready, then start a physical cycle. | Scan is cancelled; control and relay behavior are unaffected. |
| M17 | On the remote-control opt-in build, leave a browser virtual paddle ON, then close/disconnect the browser. | Machine circuit stays under shot limits / physical paddle / Stop; closing the browser alone does not open the machine circuit. |
| M18 | Reset or remove controller power while measuring relay COM/NO. | Contact remains open; there is no unintended close pulse. |
| M19 | From Ready, set **Baseline offset**, save, press **Reset learned stop offset to baseline**, confirm, then restart. | The learned offset is reset to the configured baseline (factory default 1.5 g) and remains there; a pending post-shot analysis cannot overwrite it. |
| M20 | Open the Web UI status with the external safety macros disabled. | `Circuit safety` reports timers and WDT ready, plus `external not configured`; this build is identified as software-only protection. |
| M21 | On an isolated bench load, use an instrumented test build to stop `loopTask` immediately after machine circuit closes. Do not attach OpenOCD during the measurement. | GPTimer/K2 opens the machine circuit within the configured measured limit and TWDT reboots the board within its 5 s timeout; the boot never recloses the machine circuit. |
| M22 | Freeze only `scale_worker` while leaving control responsive. | Its own TWDT subscription causes a reboot within 5 s; machine circuit opens on or before reset and does not resume afterward. |
| M23 | Freeze only `network_manager` while leaving control responsive. | Its own TWDT subscription causes a reboot within 5 s; no network task can directly restart or close the machine circuit. |
| M24 | Build with K2/feedback enabled, disconnect the isolated feedback input, and request a close. | Feedback fails to confirm within 100 ms; K1 opens and safety enters `LOCKOUT`. |
| M25 | Force feedback to the CLOSED level before boot and before a close request. | The supervisor reports `FEEDBACK_STUCK_CLOSED`, remains in `LOCKOUT`, and never energizes K1. |
| M26 | While K2 is energized, hold heartbeat permanently LOW, then permanently HIGH, and finally disconnect it. | The external detector opens K2 within its independently specified timeout in all three cases. |
| M27 | Simulate a welded K1 or shorted K1 driver using the isolated bench fixture, then remove heartbeat. | K2 still opens the series machine circuit path within its measured non-retriggerable limit. |
| M28 | Repeat M01, M02, M07, M18 and M21-M27 on ESP32-S3 N8R4 and N16R8. | Every target passes with measured timings recorded; no idle-task starvation or false WDT resets. |
| M29 | Run brownout, power-cut, Wi-Fi/RF stress and a 72 h soak followed by 10,000 relay cycles on the isolated fixture. | No spontaneous close, over-limit close, resumed cycle after reset or undiagnosed feedback mismatch occurs. |
| M30 | With the default build, authenticate from two browsers and inspect the Actions panel and API. | Without Admin unlock, Home shows the version footer and no Actions panel. Start shot, rinse, and STOP all return `ADMIN_LOCKED`. |
| M31 | With remote control deliberately enabled, unlock Admin on browser A and start a Web cycle. From browser B (no unlock), attempt paddle/rinse/stop; then tap **Lock** in the header or Admin on A. | Browser B cannot start, rinse, or STOP (`ADMIN_LOCKED`). **Lock** on A clears Admin unlock and hides Actions; further Start/Stop/rinse on A return `ADMIN_LOCKED`. The WebUI claim stays; the paddle still opens the machine circuit. |
| M31b | Unlock Admin, open Home, and leave it polling with no Start/Stop/rinse and without returning to Admin. Then unlock again, stay on Admin, and separately tap **Lock**. | After 15 min on Home only, Actions disappear and header **Lock** hides (`adminUnlocked` false). Staying on Admin or using Start/Stop/rinse renews the 15 min. **Lock** hides Actions immediately. |
| M32 | While Ready, request each configuration, Wi-Fi, scan and factory operation, then move the physical paddle before its maintenance settle time. | The operation is canceled, machine circuit stays open, state requires physical OFF, and no partial runtime setting is applied. |
| M33 | Disconnect or freeze the scale before its first weight, then repeat after allowing the last weight to become older than 1 s. | A cycle without initial scale authority remains manual; an automatic cycle with a stale stream publishes `STALE`/`SUSPENDED` and never uses stale weight. |
| M34 | During an automatic shot, inject NaN, one isolated abrupt sample, and then two fresh abrupt samples above 1,000 g as if pressing the scale with a finger. | Invalid/outlier data never enters regression or learning. One sample suspends control; a confirmed overload opens the machine circuit with `WEIGHT_ANOMALY`, even before ten regression samples exist. |
| M35 | Force the scale-command and event queues full while ending a shot. | Machine circuit opens immediately; the control loop never blocks. The scale STOP is retried within its bounded window and drop counters become visible. |
| M36 | Fail AP creation and HTTP startup independently, then remove each fault without rebooting. | Partial startup rolls back and retries with bounded backoff until the AP/UI recovers; control remains responsive and fail-open throughout. |
| M37 | Provision two factory-reset devices and record the Serial output. | Each prints a valid, different device password; neither accepts a shared/default password. |
| M38 | Force CPU-frequency setup, EEPROM initialization and BLE initialization failures one at a time on an instrumented build. | Frequency/safety initialization failure prevents machine circuit close; EEPROM failure uses volatile safe defaults without network persistence; BLE failure permits only local manual operation. |
| M39 | During a 72 h target-specific soak, record `/api/v1/status/admin` health fields and firmware size on every build. | Loop/task age and stack remain bounded, heap minimum/largest block do not trend toward exhaustion, drops are explained, and every `.bin` stays below the CI budget. |
| M40 | Boot with the onboard GPIO1 LED wired as documented and **Blue LED while scale connected** enabled. | The LED stays LOW through boot with no scale; connecting a scale drives GPIO1 HIGH without relay movement; disconnecting returns it LOW. |
| M41 | Disable **Blue LED while scale connected**, connect a scale, then re-enable the setting. | GPIO1 stays LOW while the setting is off even if the scale is connected, and goes HIGH when the setting is saved on. Machine circuit behavior remains controlled by the state machine. |
| M43b | Trigger a watchdog panic/reboot while machine circuit is closed, keep the paddle OFF after reset, and open every WebUI view. | The panic callback opens the machine circuit before reboot and `setup()` holds it OPEN; the previous cycle does not resume. The controller reaches normal Ready without a recovery gesture or unsafe override; WebUI configuration, rinse, and the next machine circuit cycle work normally. |
| M44 | Short or disconnect the GPIO1 LED, then repeat a normal paddle stop and the 60-second hard-limit test. | LED wiring failure cannot delay the control loop, keep machine circuit closed, or affect either hard deadline. |
| M45 | Start an automatic shot with the cup already on the scale before paddle ON. | Initial tare only; retare window runs but does not retare a stable full cup; first drops beep when coffee starts (independent of the retare window); shot stops at target weight after BBW protection ends. |
| M46 | Power-cycle the scale, reconnect, place a cup, and run the first paddle shot. | The first shot enters automatic brew; the log does not show `MANUAL_NO_SCALE` from missing fresh weight. |
| M47 | During early brew with rejections, inspect the Web UI log. | Each rejection names the reason (`slew`, `range`, etc.) and includes numeric weight context. |
| M48 | Run an automatic brew to target weight after M45 or M46. | Machine circuit opens at the BBW target after BBW protection ends (`normal_target`). |
| M49 | Start an automatic shot with the cup off the scale, then place a ~150 g cup within 1 s of brew start (a real put-down may overshoot, e.g. 160→150). Repeat with the cup at ~3 s (still inside the default 4 s retare window). | Automatic retare occurs once; **no first-drop beep** when the cup lands; shot timer does not restart; first drops beep once when coffee starts (against tare zero, even during **Post-tare grace**); shot stops at target weight after grace settle. |
| M50 | Repeat M49 with automatic retare disabled. | No retare window; BBW protection still runs from shot start. |
| M51 | During the retare window, place only a stable ~7 g object (below minimum cup weight in **Settings → Machine and scale → Cup**). | No retare; retare window expires; BBW protection continues until timeout. |
| M52 | Place a heavy cup (> target weight) during the retare window. | Shot does not stop by weight until BBW protection ends. |
| M53 | Let BBW protection timeout expire without detecting drops, then allow coffee to accumulate. | No beep at timeout; when first drops are later detected, optional beep plays once; weight stop then works normally. |
| M54 | Shorten BBW protection below retare window + 3 s in the Web UI and save. | Server rejects the transaction or clamps to the minimum; UI hint explains the rule. |
| M55 | With automatic retare off, run an automatic shot. | Retare window is skipped; BBW protection still inhibits weight stop until it ends. |
| M56 | Open the Web UI home, then reload and claim while a scale is connected. | First HTML load is gzip (`Content-Encoding: gzip`). JS loads from `/app.js?v=…` and CSS from `/app.css?v=…` (both gzip). Reload and post-claim reload return **304** for HTML when the firmware is unchanged; JS/CSS are cached via versioned immutable URLs. Scale stays connected; no `PACKET_TIMEOUT`. Safari does not show “Device unreachable”. |
| M57 | On home, open DevTools Network: stay on `/` for ~10 s, then open `/log`, `/history`, `/debug`, `/admin`, and `/settings`. Scroll the history table if more than 10 shots exist. | Home only polls `/api/v1/status/home`. Log polls `/api/v1/log` only while on `/log`. History polls `/api/v1/shots?offset=0&limit=10` only while on `/history`; scrolling requests later pages of 10. Admin/Debug/Settings poll `/api/v1/status/{admin|debug|settings}` only while on those views. Deep-link refresh of `/log` serves the SPA and shows the log view. Deep-link `/debug` while signed in shows Debug. |
| M58 | Sign in → Settings → Brew: confirm Single/Double cards; Load Single then Double; form goal/min/rec/baseline follow the active preset without copying into a separate machine brew blob. | Active card highlights; composed status matches the preset; reboot keeps the same active id. |
| M59 | Edit Double goal, Save preset, pull a shot that learns offset; switch to Single then back to Double. | Learned offset stays on Double; Single keeps 0.5 g baseline/offset; Reset learned offset restores that preset’s baseline. |
| M60 | New → Untitled (FW Double seed 36 g / baseline 1.5); Duplicate Double → “Double copy”; Duplicate again → “Double copy 2”; rename copy inline (desktop) or dialog (phone); Delete a custom until one remains (last delete rejected). | Names and seeds match; rename persists without rewriting learned fields; cannot delete the last preset. |
| M61 | Home: turn Brew by weight OFF (Manual), pull a timer-only cycle, turn ON again; open Settings brew checkbox. | Manual is session override and does not persist `brewByWeight=false` into the recipe; Save preset while Manual was on still keeps recipe BBW if the Settings Brew checkbox stayed on. |
| M62 | Exercise New / Duplicate / Save / Delete / Load while watching Serial for `network_manager` stack canary; repeat with Wi‑Fi clients polling status. | No reboot/canary; heap stays healthy; status `presets` JSON remains bounded. |
| M63 | On a build with `SHOT_STOPPER_ENABLE_BUZZER=2` and an active buzzer on the configured GPIO, from Ready open Debug and play short, long, double, triple, **Slow (3 s)**, **Medium (3 s)**, **Fast (3 s)**, **Rapid (3 s)**, **Chime**, **Swing**, **Echo**, **Echo inverted**, **Morse**, and **Snap**; set Alerts **Output channel** to Buzzer only, confirm **Scale connected** and **Scale lost** are visible and ON, **Extended shot pulse** and **Slow extended pulse** to Fast; trigger tare/start/first-drop/paddle-off, a scale disconnect (Echo inverted), a scale connect/reconnect (Echo), a Fast extended shot (target before min brew time) so the Fast pulse runs until cut, and optionally a Slow extend (past max brew time) for the Slow pulse; then set Scale priority and confirm Scale connected / Scale lost still play on the local buzzer, while paddle reminder prefers the scale when connected. Repeat on a `=1` passive-piezo build if available. | Each debug button matches the same on/gap catalog as the live alerts (short = single events; triple = ATM / manual-without-scale; Echo = scale connected; Echo inverted = scale lost; Slow/Medium/Fast/Rapid = looping 20 ms ticks at 2/3/4/5 per second for 3 s; Chime/Swing/Morse/Snap are compare-only motifs). Buzzer only routes alert sound to the local buzzer; Scale priority uses the scale when connected except scale-incapable cues (Scale lost/connected, triples, extended pulses). Active vs passive may differ in timbre, not in beep/gap timing. Machine circuit stays open during debug. |
| M64 | With **Avoid BBW shot without scale** ON (default), BBW ON, and the scale off: from Ready, paddle ON **past the rinse gesture**, then OFF; confirm Status **No-scale guard** goes Armed → Idle and machine circuit stays open (triple beep on paddle ON if buzzer compiled and *Manual without scale* is ON). Shot **No-scale guard** stays at the last-shot value (or — if none). Paddle ON again. Also try a **short** rinse gesture while Armed (machine circuit must close, guard goes Idle, same triple beep on paddle ON). Then reconnect the scale, or wait the configured **Last shot cooldown**. | Long first start stays `READY`, machine circuit open, Status Idle; triple on paddle ON, not after the rinse window. Second start is `MANUAL_NO_SCALE` and machine circuit closes (triple again). Short Armed gesture is `RINSE` and goes Idle (triple on paddle ON). The triple is **even** 80 ms on / 70 ms gap (not a stretched first beep). Serial debug is **off** by default (Debug checkbox / `SERIAL_DEBUG_ON` at **115200** baud); the buzzer phase timer keeps that rhythm if debug prints are on. Scale reconnect or cooldown returns Status Armed. Web rinse also consumes the guard. Idle rinse/brew are unchanged. BBW OFF or the setting OFF does not block. |
| M65 | Home Quick Settings: with Brew by weight ON, toggle **No-scale BBW**, **Fast extraction guard**, **Slow extraction guard**, **A→M time guard**, and **Cup protection**. Open Settings: the matching controls follow. In **Settings → Alerts**, turn **Sound alerts** OFF and trigger first-drop, paddle, completion, scale-connect/lost and extended-pulse events; then turn Brew by weight OFF. | Each switched Home control turns yellow and read-only until status confirms (or up to ~5 s, then snaps back). Sound alerts is Settings-only (Save machine settings), silences both output routes, and makes every other Alerts control read-only without changing its stored value. The BBW-dependent Home switches remain read-only with BBW OFF and re-enable with BBW ON. |
| M66 | In **Settings → Machine and scale → Scales**, set **Drip delay** to 0 s, run a shot, then repeat with 3.0 s and 10.0 s while watching Last Shot/history and the scale weight after machine circuit opens. Reboot after saving and verify the selected value remains. | `status/settings.config.dripDelayMs` reports 0, 3000, and 10000 respectively. Final weight/history update on the next control loop at 0 s and only after the configured window at 3/10 s. The saved value survives reboot; starting another shot during a pending window commits the previous shot with its last-known weight. |
| M66 | USB serial at **115200**: `HELP`, `DEBUG_STATUS`, `DEBUG_FULL`, `WIFI_STATUS` / `AP_STATUS` / `WEBUI_STATUS` / `NET_STATUS`, `HEALTH`, `SCALE_STATUS`, `NTP_STATUS`, `LOG_DUMP`. Then `WIFI_CONNECT` / `WEBUI_RESTART` and, with STA up, `AP_START` (SoftAP must stay up). Watch the action log. Finish with `DEBUG_OFF`. See [USB serial CLI](SERIAL_CLI.md). | `HELP` lists the new verbs. Status dumps print without a Ready gate. `DEBUG_FULL` persists serial+ring at debug; connect/start/restart print detailed OK/ERR lines even before debug is on. Automatic STA retry / SoftAP raise stay quiet until debug is on. `AP_START` while STA is connected leaves SoftAP up (`mode=AP_STA`). `DEBUG_OFF` silences traces after the OK line. |
| M67 | Open the Web UI, then leave it without using a control for 15 min while watching DevTools Network. Scroll and switch browser tabs during the wait. | At 15 min the page stays open, all controls lock, polling stops, and a full-screen overlay with the brand mark and **Reload** fades in. Scrolling and changing browser tabs do not reset the timer. Select **Reload** and confirm the overlay fades out and polling resumes for the visible view. |
| M68 | With a scale connected, open ShotStopper Companion and scan for the stopper. Exercise FF10–FF20, write SSID then password through FF22/FF23, observe FF24, write FF21, reboot through FF25, and toggle AP through FF26. Set `BLE_COMPAT_DISABLE`, verify status before reboot, reboot, inspect `HEALTH`, then re-enable and reboot again. | The app discovers `shotStopper` by service `0FFE`; the scale remains connected. Valid Ready-state settings persist, invalid/active-shot writes revert, FF21 stays false and never starts OTA, IP/AP notifications follow effective state, and FF25 reboots only from Ready. Before reboot, status reports configured disabled, active this boot, and restart required. After reboot there is no Companion advertising/client/profile allocation, BLE scale support remains functional, and heap increases. Re-enabling likewise remains pending until reboot. |
| M69 | On an isolated bench, follow the three-cycle procedure in [EMERGENCY_RECOVERY.md](EMERGENCY_RECOVERY.md), including the 3 s confirmation. | Machine circuit remains open; three 50/50 ms pulses sound; the controller restarts with STA/LKG/IP cleared and device password `ineedacoffee`, while presets, calibration and history remain intact. |
| M70 | On an isolated bench, follow the five-cycle procedure in [EMERGENCY_RECOVERY.md](EMERGENCY_RECOVERY.md). Repeat with four cycles, a gesture longer than 5 s, movement during confirmation, and expiry of the 60 s window. | Only five valid cycles produce five 50/50 ms pulses and a full factory reset. Invalid attempts erase nothing; timeout gives the 1.5 s exit beep; machine circuit never closes. |
| M71 | Start each recovery operation and cut power between intent write, individual store resets, verification and restart. Repeat on builds with passive, active and disabled buzzer. | The next boot resumes the pending idempotent reset before Wi-Fi/BLE; success data matches the documented scope. Without buzzer behavior is otherwise identical. A forced NVS failure produces the distinct error motif and keeps machine circuit open. |
| M72 | After an Arduino-ESP32 core bump (pinned **3.3.11**), on a bench S3+PSRAM board with machine circuit disconnected from the machine: boot, confirm Serial 115200 has no `ble=fail`, connect a scale with fresh weight, open the Web UI (claim, home polling, one Settings save), capture USB `HEALTH` at boot and again with UI+scale, keep AP or STA up during BLE scan, run `WEBUI_RESTART` with the scale connected. If Companion is in the build, discover service `0FFE` while the scale stays connected (reduced M68). | `BLE.begin()` succeeds; scale stream stays fresh with the browser open; Web UI loads and polls; internal `heapFree`/`heapLargest` are not exhausted and PSRAM is in use; no `health heap low`; HTTP returns after `WEBUI_RESTART` without dropping the scale. Abort and revert the core pin on `BLE.begin` fail, Web UI that will not start, internal heap with no usable large block, spontaneous reset, or a scale that dies when the browser opens. This is a ~15–30 min smoke gate, not M29/M39. |
| M73 | Same bench setup as M72: leave the scale connected and a browser on home (polling) for 2–4 h. Every 30–60 min capture USB `HEALTH` and `/api/v1/status/admin`. | Internal `heapMin` does not stair-step toward exhaustion; no burst of `PACKET_TIMEOUT`; no WDT/panic; UI keeps responding. A 72 h M29/M39 soak and shots on-machine are optional before calling the core production-validated and do not block the pin if M72 and this short soak pass. |
| M74 | On an isolated bench with machine circuit disconnected, change the device password away from the factory default, then run `./scripts/bo-idf --arch <board> --host <ip>` with a build whose `VERSION` was bumped. Watch the OTA progress, the identity the controller reports back, and the restart. | The OTA is accepted, the reported identity matches the local `./scripts/image_tag.js` output, and the controller restarts into the new version. `/api/v1/status/admin` shows the new `running` version and `confirmed: true` within ~20 s. Wi-Fi credentials, presets, calibration and shot history are unchanged. Machine circuit never closes at any point. |
| M75 | Repeat M74 from **Admin** (unlock with the device password) **→ Firmware update** in the Web UI, in two steps. | *Upload and verify* shows a progress bar and then the verified version and board; *Flash and restart* stays disabled until that verification succeeds. **Discard** returns the panel to "No verified image." without touching the running firmware. |
| M76 | Attempt an update with: the factory device password still set; a wrong token; an image built for the other board; an image older than the running one; a random non-firmware file; and a truncated `.bin`. | Each attempt is refused with a named reason (`OTA_TOKEN_INVALID`, `ARCH_MISMATCH`, `DOWNGRADE`, `BAD_IMAGE`, `VERIFY_FAILED`) and the controller keeps running its firmware. `/api/v1/status/admin` still reports the original `running` version. |
| M77 | Start an upload and interrupt it: cut Wi-Fi mid-transfer; then repeat and cut controller power mid-transfer. | Both cases end with the original firmware running after reboot, `state: idle`, and no staged image. Nothing was written to the running slot. |
| M78 | Start an upload while Ready, then move the physical paddle (or close the machine circuit) mid-transfer. | The update is cancelled with `SAFETY_LOST`; the machine's cycle behaves normally and is never delayed by the transfer. Starting an update while a cycle is already active is refused up front. |
| M79 | Flash a deliberately broken build (for example one that panics in `setup()` or cannot start HTTP) on the bench. | The controller boots it once, fails to serve the Web UI, and the bootloader returns to the previous slot. Recovery needs no USB. The diagnostic log of the recovered firmware shows `OTA rollback armed` or the panic reboot. |
| M80 | Immediately after an OTA restart, and before the new image is confirmed, attempt a second update. | It is refused with `PENDING_VERIFY`; after the Web UI has been served the update is accepted normally. |
| M81 | After flashing a build with PSRAM Web UI / Wi-Fi buffers and BLE-host PSRAM (JSON arena, NetworkWorkBuf + shared POST body, Wi-Fi scan records/snapshots, debug ring BSS; ArduinoBLE GAP/ATT/GATT host via `BLEHostAlloc`; OTA chunk, NVS scratch, and network/httpd stacks still internal; VHCI + controller still internal): capture `/api/v1/status/diagnostic` at boot, then browse Settings/Presets/Admin for 10+ min with active polling, run a Wi-Fi scan, save settings once, and complete one OTA upload. Compare `freeHeapBytes`, `largestFreeHeapBlockBytes`, and `minimumFreeHeapBytes` before and after. Confirm paddle and machine circuit still respond immediately during UI polling. Check `HEALTH` / diagnostic `bleHostAllocPsram` rises after scale connect (and Companion ON if enabled) while `bleHostAllocFallback` stays near zero when PSRAM is healthy. | Internal heap stays above health thresholds (48 KiB free / 16 KiB largest alert); `minimumFreeHeapBytes` does not stair-step down during Web UI use; no `HEALTH_HEAP_LOW` debug events; settings save, OTA, and Wi-Fi scan succeed without `NO_MEMORY` or cache-disabled panics. `psramFreeBytes` is below total PSRAM (Web UI / Wi-Fi / BLE host work buffers are placed there). Paddle GPIO, relay, VHCI, and BLE controller stay on internal SRAM. |

Record firmware version, board/FQBN, scale model/firmware, measured timings, relay continuity and pass/fail evidence for every run.
